[package]
name = "Recursion"
version = "0.1.0"
description = "Demonstrates recursive functions and tail recursion"
authors = ["SpeakEZ Technologies, Inc."]
license = "MIT"

[compilation]
target = "native"

[dependencies]
# Types and operations are FNCS intrinsics - no external library needed
platform = { path = "/home/hhh/repos/Fidelity.Platform/CPU/Linux/x86_64" }

[build]
sources = ["Recursion.fs"]
output = "recursion"
output_kind = "console"
