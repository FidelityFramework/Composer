# Expected outcome for W-5/reject. Read by vet.sh; keys are one-line strings.
rule = "a seal must cover the analysed range"
verdict = "reject"
code = "CCS8012"
count = "2"  # the code must appear at least this many times (one per reject site)
flags = "--warnaserror"  # extra compile flags; a coverage finding is a Warning promoted under --warnaserror
pending = "step 7"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 7
