# Expected outcome for W-5/accept. Read by vet.sh; keys are one-line strings.
rule = "a seal must cover the analysed range"
verdict = "accept"
code = ""
pending = "step 7"  # the hardening step (plan §5) that gates this row; judged only when vet.sh --through >= 7
