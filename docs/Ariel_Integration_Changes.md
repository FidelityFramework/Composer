# Ariel integration: repository changes

Working-tree changes are uncommitted. These suggested one-line messages cover
the current typed native boundary, mapped storage and multi-core CPU work.
They do not include unrelated pre-existing changes in these repositories.

| Repository | Suggested commit message |
| --- | --- |
| clef (`clef-scope-integration` worktree) | Carry native layouts, callback evidence and scoped borrowed views through CCS and Baker |
| Composer | Lower typed foreign boundaries and scoped mapped storage through LLVM and LLD |
| BAREWire | Describe typed foreign ownership, callbacks and borrowed mappings with focused metadata projects |
| Farscape | Generate typed pthread and display bindings with ABI projections and scoped GBM mappings |
| Fidelity.Platform | Add persistent Ariel CPU carriers and generated Linux display bindings |
| HelloWayland | Render the CPU Wayland window through Ariel carriers and scoped BAREWire mappings |

See [the implementation and acceptance record](multi-core-cpu.md) and
[HelloWayland's application record](../../HelloWayland/docs/multi-core-cpu.md).
The actual animated window acceptance remains pending; the native mapping,
scope, lifecycle, renderer-equivalence and conditional span-proof gates are
recorded separately. Update that acceptance status after the live window,
worker activity, resize and shutdown checks complete.
